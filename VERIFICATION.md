# 驗證方式與流程（生活科技平台）

新增內容的通則：**每一層都要能自動重跑**，不靠肉眼看過就算數。
分成四層 —— L0 授權、L1 靜態、L2 邏輯、L3 實測、L4 教學。任何一層紅燈就不推上線。

---

## L0 授權驗證（本專案的第一道關卡）

**為什麼放第一**：內容做完才發現不能公開，等於全白做。

| 檢查項 | 方式 | 通過標準 |
|---|---|---|
| 素材來源歸屬 | 逐檔標記 `自有 / 翰林(本人著作) / 開源(附授權) / 他人教材` | 沒有任何「他人教材」進入 repo |
| 文字原創性 | 新寫文案與碟上教材做字串比對抽查 | 無整句照抄 |
| 引用來源 | 只引用公開標準／官方文件／政府課綱，寫進 `js/sources.js` | 每條有可驗證 URL |
| 圖像 | 一律自繪 SVG／Canvas，不使用他人圖檔 | repo 內無外來圖片 |

**RAG 語料特別注意**：`RAG後的資料/documents/custom-documents/`（679 檔 / 1845 chunk）混有他人版權物（含出版社課本章節、Springer 專書），
**不可**用於公開 AI 助教。做助教時必須以白名單重建索引：
`livingtech.education`（原站）＋`livingtech-tools`（自有）＋翰林（本人著作）＋開源技術文件＋naer 課綱。

---

## L1 靜態驗證（每次存檔後）

```bash
cd ~/3d/livingtech-tools
# JS 語法
for f in emerging-tech/js/*.js js/*.js; do node --check "$f" || echo "FAIL $f"; done
# 引用資源是否存在
grep -ohE '(href|src)="[^"]+"' emerging-tech/**/*.html | grep -vE 'https?://|data:|#'
```

通過標準：語法零錯誤、所有相對路徑檔案存在、導覽鏈 module1→…→5→首頁 不斷。

---

## L2 邏輯驗證（**本專案抓到 3 個真 bug 的那一層**）

模擬器不是動畫，是會被學生當成事實的模型。三件事必須用程式窮舉證明：

1. **闖關可達性** — 每個挑戰在滑桿／選項的全空間中至少有一組解
2. **數學正確性** — 對照已知封閉解或標準值
3. **教學張力真實存在** — 該有取捨的地方，取捨真的會發生

實作方式：把模組的核心函式抽成獨立 node 腳本窮舉（見 `scratchpad/verify_m*.js` 模式）。

**本次實際攔截到的問題（若只靠肉眼會全部漏掉）：**

| 模組 | 問題 | 後果 | 修法 |
|---|---|---|---|
| M1 AI辨識 | `q_robust` 在全空間 **0 組解**（遮擋30%上限70.5%，門檻卻設80%） | 學生卡死、永遠無法解鎖 M2 | 遮擋係數 .85→.75、門檻 80%→65%；重驗得 84 組解 |
| M4 綠建築 | 舒適違規在 16×8 組合中 **恆為 0** | 「節能vs舒適」的教學張力是假的，會教出錯誤直覺 | 新增兩條積極策略；重驗得 60 組會違規，積極省60.4%但有違規、保守省32.7%零違規 |
| M5 穿戴 | `q_good`（跑步≤10bpm）**0 組解**；靜止配戴不良竟達 41.8% 誤差（不合實際） | 最後一關無法完成；數值誤導 | 靜態因素改為隨運動強度加權；重驗 80 組解，理想配戴 0.6%→6.9% 遞增 |

驗證輸出應包含：可達組合數、範例解、單調性、邊界值（如讀值不得為負）、機率總和＝1。

### 進階：直接測「出貨的程式碼」而非重寫的原型

重寫一份原型來驗證，只能證明「我的原型對」，不能證明「網站上跑的那份對」。
更強的做法是用 mock DOM 把實際的 `module*.js` 載進 node，直接驅動它的函式：

```js
const vm=require('vm');
const src=fs.readFileSync('mechatronics/js/module3.js','utf8');
// const 宣告不會掛到 sandbox，需在尾端附加匯出行
vm.runInContext(src+'\n;globalThis.__X={params,step,newCar,PERIM};', sandbox);
```

`mechatronics/module3` 即以此法驗證，結果與原型完全一致（2896 組完成、最快 6.20 s）。

### 設計順序：先驗證物理，再寫 UI

`mechatronics/module3` 改用「先在 node 把物理模型調到滿意，再寫介面」的順序，
過程中連續發現三次模型不足，每次都是靠掃描而非肉眼發現的：

1. 無馬達飽和 → Kp 再大都不失敗
2. 加了飽和仍無上界 → 純運動學 + P 控制**數學上無條件穩定**，缺的是<strong>延遲</strong>
3. 加入一階馬達響應延遲（τ=0.30）後，才出現真實的調校張力

最終得到一個被程式證明的教學機制：**速度 ≥100 且 Kd=0 的可完成組合＝0**，
學生想跑快就<strong>必須自己發現微分項</strong>。

### 第二輪實戰戰績（mechatronics）

| 模組 | 問題 | 後果 | 修法 |
|---|---|---|---|
| M4 超音波 | 音速溫度補償<strong>比例寫反</strong>（寫成 c實際/c假設） | 低溫顯示低估，實際應高估——物理方向相反，會教錯 | 改為 c假設/c實際；以「回音時間獨立推導」交叉驗證（-5°C→+9.2cm、45°C→-8.5cm） |
| M2 二值化 | 深灰組合分離度僅 246 卻顯示綠燈 | 學生誤以為這種組合安全 | 警戒門檻 120→300，讓它正確示警 |
| M1 文案 | 混入英文 `need`（前一輪曾混入俄文 `можно`） | 顯示異常 | L4 掃描攔截 |

---

## L3 實測驗證（瀏覽器）

```bash
# 用 preview 工具啟動（勿用 Bash 跑 server）
# .claude/launch.json 已設定 name: livingtech-tools, port 8123
```

流程：
1. `preview_start {name:"livingtech-tools"}`
2. `read_console_messages` → 必須 **零 error**
3. `read_page` → 確認模組卡片、挑戰清單、canvas 有渲染
4. 實際操作滑桿／點選項 → 確認挑戰會亮燈、`next-btn` 會解鎖
5. `localStorage` 檢查 `et_progress_v1` 有正確寫入、重整後進度保留
6. `resize_window` mobile(375) → 確認 canvas 不溢出、控制項可點
7. 截圖存證

> **目前狀態：L3 尚未執行**（preview 服務因分類器故障暫時不可用）。L0–L2 已全數通過。

---

## L4 教學驗證（上線前）

| 檢查項 | 方式 |
|---|---|
| 課綱對應 | 每模組 badge 標示的條目與課綱原文核對 |
| 用語一致 | 與既有模組（微控制器／麵包板）術語不衝突 |
| 難度落點 | 挑戰 1 應在 30 秒內可達；挑戰 3 需思考 |
| 錯字／混語 | 全文掃 `[а-яА-Я]`、日文假名等非預期字元（本次曾誤混俄文 `можно`） |
| 教師可用性 | 每模組能否在一節課內完成 |

```bash
# 非預期語系字元掃描（排除 U+30FB 間隔號「・」— 站上既有慣例）
grep -rnP '[\x{0400}-\x{04FF}\x{3040}-\x{30FA}\x{30FC}-\x{30FF}]' emerging-tech/ || echo "✅ 無混語"
```

---

## 每次新增內容的標準流程

```
L0 授權盤點 → 建檔 → L1 靜態 → L2 窮舉邏輯 → L3 瀏覽器實測 → L4 教學檢查 → commit
```

**紅線**：L2 未通過不得進 L3；L0 有疑慮直接停工重新設計素材來源。
